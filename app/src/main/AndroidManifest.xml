<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vishalinventory.physicalinventory">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/vishalnew"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/vishalnew"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:configChanges="keyboard|keyboardHidden"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ScanActivity"
            android:configChanges="keyboard|keyboardHidden"
            android:label="Scanning Process"
            android:screenOrientation="portrait" />
        <activity
            android:name=".PhysicalcountActivity"
            android:configChanges="keyboard|keyboardHidden"
            android:label="PhysicalCount"
            android:screenOrientation="portrait"></activity>
        <activity android:name=".OldCsvData"></activity>
    </application>

</manifest>